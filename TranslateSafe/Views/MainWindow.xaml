<Window x:Class="TranslateSafe.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TranslateSafe (Steuerzentrale)" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Status / Letzte Übersetzung:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Background="#F0F0F0">
            <TextBlock Text="{Binding TranslatedText}" 
                       TextWrapping="Wrap" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Foreground="#333333"/>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Speichern (für Flashcards)" 
                    Command="{Binding SaveCommand}"
                    Padding="10,5"
                    Margin="0,0,10,0"
                    Foreground="White"
                    Background="#007ACC"/>
            <Button Content="Flashcard Ansicht (Später)" 
                    Padding="10,5"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>